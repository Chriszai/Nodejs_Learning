// Express
/* 第三方web开发框架
   高度封装了http模块
   更加注重与业务 而非底层细节 要知其所以然
   
   MongoDB （所有方法已经封装好）*/

// 遍历jquery元素 可以在不兼容foreach的低版本浏览器使用
$.each(['abc','b','c'],function(index,item){
})

// ie8不支持
['abc','b','c'].forEach((element,index,) => {  
});